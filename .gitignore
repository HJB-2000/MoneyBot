# Claude Code local files — never push
.claude/

# Virtual environment
.venv/
venv/
env/

# Local data — stays private, too large for GitHub
data/historical/
data/*.csv
data/*.db
moneyBot.db

# Ignore all JSON in data/ except trained model outputs
data/*.json
!data/likelihood_ratios.json
!data/base_rates.json
!data/signal_correlations.json
!data/signal_weights.json
# live_signals_30.json is runtime ephemeral, never commit
data/live_signals_30.json

# Logs — local only
logs/

# Secrets / env
.env
*.env

# Python cache
__pycache__/
*.py[cod]
*.pyo
.pytest_cache/
*.egg-info/
dist/
build/

# Reports / local output
reports/

# Misc
*.tmp
*.bak
dashbord.text
plan.md
server.md
